import{describe,expect,it}from'vitest';import{costVariance,menuClass,temperatureCompliance}from'./cuisine-quality.rules';describe('qualité cuisine',()=>{it('contrôle les plages de température',()=>{expect(temperatureCompliance(3,0,4)).toBe(true);expect(temperatureCompliance(8,0,4)).toBe(false);});it('classe le menu',()=>{expect(menuClass(12,10,500,400)).toBe('star');expect(menuClass(4,10,500,400)).toBe('puzzle');expect(menuClass(12,10,200,400)).toBe('workhorse');expect(menuClass(4,10,200,400)).toBe('dog');});it('calcule l’écart de coût',()=>expect(costVariance(1000,1150)).toEqual({amount:150,percent:15}));});
